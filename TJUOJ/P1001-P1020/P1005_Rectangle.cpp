#include <bits/stdc++.h>
using namespace std;

int main()
{
    int length, breadth;
    cin >> length >> breadth;
    cout << length * breadth << " ";
    cout << length * 2 + breadth * 2 << endl;
    return 0;
}